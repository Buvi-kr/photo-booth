{
  "permissions": {
    "allow": [
      "Bash(powershell \"Get-ChildItem -Recurse -Filter '*.cs' | Sort-Object LastWriteTime -Descending | Select-Object -First 20 | Format-Table Name, LastWriteTime, Directory -AutoSize\")"
    ]
  }
}
